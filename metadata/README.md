# Metadata

Run the following command after preparing local OASIS slices:

```bash
python scripts/write_splits.py --oasis_dir data/oasis --out_dir metadata
```

This will create deterministic subject split lists for OASIS and OASIS-SynthT2.
